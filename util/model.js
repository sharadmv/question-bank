var model = {
    Code : {
        Output : function(heap, globals, stdout) {
            this.heap = heap;
            this.globals = globals;
            this.stdout = stdout;
        }
    }
}
module.exports = model;
